<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.serve.mapper.LabelMapper">
    <select id="queryArticleIdByLabel" resultType="java.lang.Long">
        select article_id
        from blog.tag left outer join blog.article_tag on tag.id = article_tag.tag_id
        <where>
            and name like concat('%', #{name}, '%')
        </where>
    </select>
</mapper>